import { describe, expect, it } from 'vitest'

import { TEXTURE_NAMES, textureCatalog, textureRegistry } from '../index';

describe('textures registry', () => {
  it('exposes all textures from '../index';

